# Research database (Index's own DB)
DATABASE_URL="postgresql://user:pass@host/db"

# Community auth origin (for session validation)
AUTH_ORIGIN="http://localhost:5173"
ORIGIN="http://localhost:5173"
